# LLM Coding Agent V4.1 ICS — complete direct dependencies
# Python >= 3.10
# Recommended install in a dedicated virtual environment:
#   python -m pip install -r requirements.txt

# Runtime
PyYAML>=6.0,<7.0
httpx>=0.27,<1.0
python-dotenv>=1.0,<2.0
pydantic>=2.6,<3.0
tomli>=2.0; python_version < "3.11"

# Tests / quality gates
pytest>=8.0,<10.0
ruff>=0.4,<1.0
mypy>=1.9,<3.0

# Build tooling
setuptools>=68
wheel>=0.45
